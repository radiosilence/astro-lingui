pre-commit:
  parallel: true
  commands:
    oxlint:
      run: npx oxlint {staged_files}
    oxfmt:
      run: npx oxfmt --write {staged_files} --no-error-on-unmatched-pattern
      stage_fixed: true
    typecheck:
      run: npm run typecheck
